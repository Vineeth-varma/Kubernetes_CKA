## Docker Commands and Information
* docker run ubuntu         # To run a docker container
* docker ps                 # To list running containers
* docker ps -a              # To list all containers including that are stopped
